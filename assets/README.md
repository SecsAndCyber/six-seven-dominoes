# Assets

Store game assets here. Suggested subfolders:
- `images/` — sprites, atlases, PNGs, SVGs
- `audio/` — music, SFX (OGG, WAV)
- `fonts/` — TTF/OTF fonts
- `ui/` — UI images and icons

Use descriptive filenames and keep sizes optimized for mobile.
